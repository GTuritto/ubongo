"""Self-improvement (genetic programming) layer.

Phase 16 ships variant *generation* only: `targets` (the evolvable-target
registry), `generator` (mutation strategies), and `lineage` (persistence to
`evolution_lineage`). Evaluation/fitness (Phase 17), the autonomous loop
(Phase 18), and promotions (Phase 19) build on these.
"""
